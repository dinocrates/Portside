{
  "name": "portside-motion-lab",
  "version": "0.1.0",
  "private": true,
  "description": "Portside Motion Lab - ENGR-120 kinematics simulator (gantry crane trolley motion)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "phaser": "^4.0.0-beta.10",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
